spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study_platfrom1?useSSL=false&serverTimezone=UTC
    username: root
    password: Sharanu@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
